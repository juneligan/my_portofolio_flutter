import 'package:flutter/material.dart';

class AppSpacing {
  static const EdgeInsets screenPadding = EdgeInsets.symmetric(horizontal: 20, vertical: 40);
  static const EdgeInsets sectionPadding = EdgeInsets.all(20);
}